<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Allow app to check if any other app can handle URLs -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Voice input: raw mic capture for on-device STT (SttRecorderModule) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Keeps a model run alive while the app is backgrounded. The
         service itself is declared below with foregroundServiceType
         dataSync; POST_NOTIFICATIONS (API 33+) gates its notification
         visibility, not its ability to run. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Needed only for devices running Android 9 (API 28) or lower -->
    <!-- Currently used for storing chat sessions (/Download) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="true"
      android:dataExtractionRules="@xml/backup_rules_12_plus"
      android:fullBackupContent="@xml/backup_rules_legacy"
      android:networkSecurityConfig="@xml/network_security_config"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|density"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Host-scoped deep link for the Hugging Face "Use this model" flow.
             Scoped to host=hub so this is not a bare-scheme handler. Two
             schemes: pocketmind (ours) and pocketpal (external hub pages
             still emit it, keep receiving them). -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="pocketmind" android:host="hub" />
            <data android:scheme="pocketpal" android:host="hub" />
        </intent-filter>
        <!-- Share sheet: receive plain-text shares from other apps. The
             text is parked for the chat input, never auto-sent. -->
        <intent-filter>
            <action android:name="android.intent.action.SEND" />
            <category android:name="android.intent.category.DEFAULT" />
            <data android:mimeType="text/plain" />
        </intent-filter>
        <!-- Text selection menu (PROCESS_TEXT): "Ask PocketMind" entry for
             selected text. Same parking flow as SEND. -->
        <intent-filter>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <category android:name="android.intent.category.DEFAULT" />
            <data android:mimeType="text/plain" />
        </intent-filter>
        <!-- Checkout callback for the in-app PalsHub purchase flow. Scoped to
             host=checkout so it is routed to AuthSessionModule, not the
             hub deep-link path. The scheme stays pocketpal: the redirect is
             produced server-side by the PalsHub site. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="pocketpal" android:host="checkout" />
            <data android:scheme="pocketmind" android:host="checkout" />
        </intent-filter>
      </activity>

        <!-- Foreground service that keeps an in-progress model run
             alive when the app is backgrounded. Started/stopped from
             JS via ForegroundServiceModule. -->
        <service
            android:name=".AgentRunService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Foreground service that keeps the process alive while the
             local OpenAI-compatible API server is serving. The HTTP
             socket itself is owned by ApiServerModule. -->
        <service
            android:name=".ApiServerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Optional native library for OpenCL (GPU acceleration) -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />

        <!-- Optional native library for Hexagon DSP (NPU acceleration) -->
        <uses-native-library
            android:name="libcdsprpc.so"
            android:required="false" />
    </application>

</manifest>
